import { ComputeFieldDirective } from './compute-field.directive';

describe('ComputeFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ComputeFieldDirective();
    expect(directive).toBeTruthy();
  });
});
